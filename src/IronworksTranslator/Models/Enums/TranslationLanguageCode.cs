namespace IronworksTranslator.Models.Enums
{
    public enum TranslationLanguageCode
    {
        Japanese = 0,
        English,
        German,
        French,
        Korean, // The order until here should be same as ClientLanguage for compatibility
    }
}
